# ==========================================================
# FastAPI Starter Kit Configuration
# 复制此文件为 .env 并根据需要修改配置
# ==========================================================

# --- 应用基础配置 ---
# 环境: local, development, production
ENVIRONMENT=local
PROJECT_NAME="FastAPI Starter Kit"

# --- 安全配置 (Security) ---
# 警告: 生产环境必须修改此密钥!
# 运行 `openssl rand -hex 32` 生成一个新的密钥
SECRET_KEY=changeme_please_this_is_unsafe_secret_key
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# --- 跨域配置 (CORS) ---
# 允许的来源列表, 用 JSON 格式数组或逗号分隔的字符串
# 示例: ["http://localhost:3000", "https://myapp.com"]
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:8080"]

# --- 数据库配置 (Postgres) ---
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=app
POSTGRES_PORT=5432

## --- Redis 配置 ---
REDIS_PORT=6379
# RedisInsight 管理界面端口
REDIS_INSIGHT_PORT=8001
# 若不填则默认为空，生产环境必须设置
REDIS_PASSWORD=
# 本地开发用 localhost, Docker 内部互联用 redis
REDIS_HOST=localhost
# Redis 数据库索引 (默认 0)
REDIS_DB=0